import { Grid } from '@mui/material';

interface ProfileFormProps {
    formik: any;
}

const ProfileFormPreference = ({ formik }) => {
    return (
        <Grid container columnSpacing={2} rowSpacing={2}>

        </Grid>
    );
};

export default ProfileFormPreference;
